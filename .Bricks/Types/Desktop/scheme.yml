version: '3.3'
services:
  desktop:
    build:
      context: .
      dockerfile: ../../../.Bricks/Containers/Desktop/${project_os}/${project_os_version}/foundation
    image: custom-${project_os_lowercase}-${project_os_version}-v1
    hostname: desktop
    tty: true
    container_name: ${project_name}-desktop
    ports:
      - 127.0.0.1:10100:10100
    networks:
      - net

networks:
  net:
    name: ${project_name}-net
    driver: bridge
