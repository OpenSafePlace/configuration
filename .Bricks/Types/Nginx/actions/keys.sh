#!/bin/bash

# General process
## Output keys
info "[c:nginx]+[u:private]+[p:sudo] => ['${global__project_keys["[c:nginx]+[u:private]+[p:sudo]"]}']"
info "[c:nginx]+[u:private]+[p:htpasswd] => ['${global__project_keys["[c:nginx]+[u:private]+[p:htpasswd]"]}']"
